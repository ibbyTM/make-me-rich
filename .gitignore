node_modules/
dist/
# Persistent local PGlite store for the Requirements Register (src/db/pglite.ts).
# Local dev state, not a fixture — regenerated automatically (seeded on first use).
.data/
.env
.env.local
*.log
coverage/
.DS_Store
# Cached national VOA business-rates download (scripts/voa-rates-fetch.ts) —
# ~90MB compressed / ~730MB uncompressed, re-downloaded on demand, never
# committed. Only the small filtered dashboard/data/voa-rates-comparables.json
# output is checked in.
.cache/
